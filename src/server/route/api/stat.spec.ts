
export const API_spec_$path = {
  path: '/api/stat/:path*',
  params: {
    path: [] as string[],
  },
};


/*

export const API_spec_$path_THINGY_FOO = endpoint({
  method: Method.GET,
  path: '/api/character/:charId/skill/:skillId/:skillPath*',
  params: {
    charId: pvar<string>(),
    skillId: pvar<number>(),
    skillPath?: pvar<string[]>(),
    pretty?: qvar<boolean>(),
  },
  response: response<{

  }>(),
})



 */
